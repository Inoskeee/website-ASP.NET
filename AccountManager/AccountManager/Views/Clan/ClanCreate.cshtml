@model AccountManager.Models.ClanCreateModel

@{
    ViewBag.Title = "ClanCreate";
}

<h2>Создание клана</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Введите данные</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <label class="control-label col-md-2">Название клана:</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.ClanName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.ClanName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Создать" class="btn btn-primary" />
                <a href="../" class="btn btn-default mt-n2 pb-2">Отмена</a>
            </div>
        </div>
    </div>
}
